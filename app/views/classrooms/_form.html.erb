<%= form_for [@school, @classroom] do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :size %>
	<%= f.number_field :size %>

	<%= f.submit nil, class: "submit-classroom-btn btn" %>

<% end %>